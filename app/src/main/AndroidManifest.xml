<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hackrice8.hmm.hearaid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".usefulobsoletecode" />
        <activity android:name=".SecondQuestionActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FirstQuestionActivity" />
        <activity android:name=".ThirdQuestionActivity" />
        <activity android:name=".FourthQuestionActivity" />
        <activity android:name=".FifthQuestionActivity" />
        <activity android:name=".SixthQuestionActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".MultipleChoiceAnswerActivity" />
        <activity android:name=".CalibrationActivity" />
        <activity android:name=".RandomLetterActivity" />
        <activity android:name=".EyeResultActivity"></activity>
    </application>

</manifest>